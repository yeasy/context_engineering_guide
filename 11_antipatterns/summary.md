## 本章小结

本章系统地梳理了上下文工程中的三大类反模式：过度压缩、上下文污染和检索失效。

### 关键教训

1.  **压缩的代价**：不仅仅是信息丢失，还有模型推理能力的下降。不要为了省那一点 Token 而牺牲核心逻辑。
2.  **隔离的必要性**：不要高估模型的"注意力机制"。给它太多不相关的信息，它一定会分心。
3.  **检索的局限**：相似度不等于相关性。不要盲目依赖向量检索，务必保留关键词匹配等传统手段作为兜底。

识别并避免这些反模式，是成为高级上下文工程师的必经之路。
